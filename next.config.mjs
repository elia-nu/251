/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org', 'www.uber-assets.com', 'raw.githubusercontent.com', '251communications.com'],
    },
};

export default nextConfig;
